export enum BackendMapperPagination {
    id = 'backend.id',
    slug_name = 'backend.slug_name',
    route = 'backend.route',
    method = 'backend.method',
    body = 'backend.body',
    query = 'backend.query',
    create_at = 'backend.create_at',
}